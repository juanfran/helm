# Optional SQLite path, resolved from the Helm repository root.
DATABASE_URL=./data/helm.db

# Helm listens only on this machine by default.
HOST=127.0.0.1

# Set to 1 only when intentionally accepting the risks of non-loopback exposure.
HELM_UNSAFE_ALLOW_REMOTE=0
